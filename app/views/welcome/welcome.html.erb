<h2>Comienza a participar</h2>

<div class="user-permissions">
  <p><%= t("welcome.welcome.user_permission_info") %></p>
  <ul>
    <li><span class="icon-check"></span>&nbsp;Ver las propuestas de tus vecinos(as)</li>
    <li><span class="icon-check"></span>&nbsp;Comentar y compartir las propuestas</li>
    <li>
      <% if current_user.level_two_or_three_verified? %>
        <span class="icon-check"></span>
      <% else %>
        <span class="icon-x"></span>
      <% end %>
      Subir tus propuestas
    </li>
    <li>
      <% if current_user.level_three_verified? %>
        <span class="icon-check"></span>
      <% else %>
        <span class="icon-x"></span>
      <% end %>
      Votar por tus propuestas favoritas, evaluadas como factibles por el municipio
    </li>
  </ul>
  <p>
    <span>
      *Para subir tus propuestas y votar,
      <strong>sólo necesitas verificar tu identidad con tu INE</strong>
      actualizada en San Pedro Garza García.
    </span>
  </p>
  <% if current_user.level_three_verified? %>
    <p class="already-verified">
      <span class="icon-check"></span>
      <%= t("account.show.verified_account") %>
    </p>
  <% else %>
    <%= link_to(t("welcome.welcome.user_permission_verify_my_account"), verification_path, class: "button success radius expand") %>
  <% end %>

  <p>
    <% if feature?(:help_page) %>
      <%= link_to t("welcome.welcome.go_to_index"), help_path %>
    <% else %>
      <%= link_to t("welcome.welcome.go_to_index"), root_path %>
    <% end  %>
  </p>
</div>
